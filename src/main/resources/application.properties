spring.application.name=clinic-booking-app

spring.datasource.url=jdbc:postgresql://localhost:5432/clinic_db
spring.datasource.username=clinic_user
spring.datasource.password=clinic_pass
spring.datasource.driver-class-name=org.postgresql.Driver

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

server.port=8080

clinic.default-patient-cap=45
clinic.default-admin-cap=60

# ---------------------------------------------------------------------------
# New this session - shown in the push notification body. Same text
# currently hardcoded separately on the frontend's result screen (Session
# Day4/3) - worth centralizing later (e.g. have the frontend fetch this
# from the backend instead of its own copy), not required for this session.
# ---------------------------------------------------------------------------
clinic.arrival-window-text=9 AM - 12 PM

clinic.test-runner.enabled=false

jwt.secret=iMwswqjMWwsl5hUyfmNod8RW0XKRc+saGubvqZarxUs=
jwt.expiration-ms=86400000
otp.expiry-minutes=5

admin.username=clinicadmin
admin.password=ChangeThisSecurePassword123

logging.level.com.clinic.booking=DEBUG
logging.level.org.hibernate.SQL=DEBUG